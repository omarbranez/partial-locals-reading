Information About the Post
<%= render partial: 'authors/author', locals: {post_author: @author} %>
<%# first key value pair notes which partial to render. second key value pair notes what to use as local variables in the partial %>
<%= @post.title %>
<%= @post.content %>
